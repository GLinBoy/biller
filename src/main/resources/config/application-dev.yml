spring:
  config:
    activate:
      on-profile: dev
  data:
    mongodb:
      host: localhost
      port: 27017
      database: bills
      username: root
      password: password
      authentication-database: admnin
